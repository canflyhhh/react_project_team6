import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#ed6c02',
    },
    secondary: {
      main: '#e65100',
    },
  },
});

export default theme;
